<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Quantify" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Quantify" %>">
    <%= stylesheet_link_tag "//cdnjs.cloudflare.com/ajax/libs/normalize/2.1.0/normalize.css", media: "all" %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
    <%= javascript_include_tag "vendor/custom.modernizr" %>
    <link href='http://fonts.googleapis.com/css?family=Istok+Web:400,700,400italic' rel='stylesheet' type='text/css'>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <div id="container" class="container">
      <header>
        <%= render 'layouts/messages' %>
        <%= render 'layouts/navigation' %>
      </header>
      <div id="main" role="main">
        <%= yield %>
      </div>
      <footer>
        <%= render 'layouts/footer' %>
      </footer>
    </div> <!--! end of #container -->
  </body>
</html>
